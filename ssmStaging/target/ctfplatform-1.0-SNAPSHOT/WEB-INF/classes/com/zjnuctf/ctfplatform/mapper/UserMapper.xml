<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjnuctf.ctfplatform.mapper.UserMapper">

	<insert id="add" keyColumn="id" useGeneratedKeys="true">
		INSERT INTO user(name,email,age,bornDate) values (#{name},#{email},#{age},#{bornDate})
	</insert>
	<update id="update">
		UPDATE `user` SET `name`=#{name},email=#{email},age=#{age},bornDate=#{bornDate} WHERE id=#{id}
	</update>
	<select id="select" parameterType="int" resultType="User">
	  SELECT * FROM USER WHERE ID = #{id}
	</select>
	<select id="selectall" parameterType="int" resultType="User">
	  SELECT * FROM USER
	</select>
	<delete id="delete" parameterType="long">
		DELETE FROM `user` WHERE id = #{id}
	</delete>
	
</mapper>	